cargo-features = ["strip"]

[package]
name = "lipl-display-egui"
version = "0.1.1"
edition = "2021"

[profile.release]
lto = true
opt-level = "z"
strip = true

[dependencies]
eframe = { version = "0.16", default-features = false, features = ["egui_glow"] }
lipl-gatt-bluer = { git = "git://paulmin.nl/lipl-gatt-bluer.git" }
log = "0.4"
simple_logger = "2"
